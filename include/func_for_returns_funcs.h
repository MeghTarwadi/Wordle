#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <stdio.h>
#include <stdlib.h>

// Function prototypes
int length(char *word);
int strToInt(int length, char num[length]);
int validate(int number);

#endif // FUNCTIONS_H
